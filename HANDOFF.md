# ルート（ポータル） — 引き継ぎメモ

## 「データが消えた」対策 — 保存の見はり＋バックアップ配布（2026-09-16）
利用者から「レキシバトルやキッズタイピングのデータが消える」と連絡。原因の見立て:
- **localStorage は kagasen.github.io の全アプリで分け合う（Safari/iPad は約5MB）**。思考ツールの写真・教室サポートボードの手書き・
  ポータルの `kagasen_click_アプリ_日付`（毎日1つずつ増えて消されない）がうめると、ほかのアプリの保存が失敗する。
- ほぼ全アプリが保存失敗を `catch(e){}` で黙って捨てていた → 子どもは遊んだのに次に開くと戻っている＝「消えた」。
- アプリの外の原因（Safari の履歴消去・プライベートブラウズ・端末初期化）は防げない。最後の保険はファイルのバックアップ。

### やったこと
- **`hozon-guard.js`（原本）＋ `build-hozon.mjs`** … 見はりを「localStorage に書く全ページ」の `<head>` 先頭へ**埋めこむ**
  （浮きボタンと同じ貼るだけ方式。オフラインでも必ず先に動く）。目印は `kagasen-hozon` のコメント。対象ページは build-hozon.mjs の一覧。
  1. `setItem` が失敗 → 古い日付の `kagasen_click_*` だけ掃除して1回やりなおす
  2. だめなら画面上に赤いおしらせ（子どもむけ＋おとなむけ1行＝使用量と大きい順3アプリ）。**例外はそのまま投げる＝アプリの動きは変えない**
  3. 開いたときも古い `kagasen_click_*` を掃除 ／ 4. `navigator.storage.persist()` を1回たのむ（Firefox は許可ダイアログが出るのでたのまない）
  - ★子どもの記録は1つも消さない。ページで自前のおしらせを出すときは `window.KAGASEN_HOZON_QUIET = true`（typing/shop.html）。
  - キー名→アプリ名の表は hozon-guard.js の `NAMES`。記録を持つアプリを作ったら **NAMES と build-hozon.mjs の対象に1行ずつ足す**。
- 教室サポートボードの手書き保存を約半分に（座標を整数化・同じ点をとばす。形式は同じ）。
- backup-kit（v2・既存と同一ファイル）を **レキシバトル・せかいをまわろう・都道府県の冒険・言葉さがし** に配布（💾ボタン）。

### 直したら
`node build-hozon.mjs`（ページ差しかえ＋sw.js の CACHE を +1。コミット前に何回走らせても +1 だけ。りかの島は sha1 で出しなおし）→ `node build-hozon.mjs --check`。

### 検証（localhost・2026-09-16）
保存場所をうめて保存 → 赤いおしらせ・例外はアプリへ。古い印があれば掃除→保存成功・おしらせなし・今日の印は残る。
レキシバトルのバックアップ読みこみ→置きかえ→再読みこみで反映・`_mae` 退避あり。ほか3アプリもプレビュー表示OK。
対象ページ21枚をひらいて見はりが head 先頭にあることを確認。release-check エラー0／build-seo 変化なし。**iPad実機は未確認**。

### のこり（未着手）
- **りかの島（11アプリ・セーブ共有）にバックアップ未配布**。build-islands.mjs の生成物で、src に作業中の変更があったため見送り。
  見はりだけは src/00-head.html と生成物の両方に入れてある。
- ✅ 第3段（同日）: 思考ツールのページ・教室サポートボードの手がきを **IndexedDB** へ（共通部品 `kioku-db.js`・はじめて開いたときに自動引っ越し）。
  くわしくは各アプリの HANDOFF。localStorage は子どもの小さな記録だけになる。
- 端末の localStorage が空くのは、その端末で新しい版の思考ツール／ボードを**いちど開いたとき**。

## ホームの題字をカラフルに・キャッチコピー刷新（2026-09-08）
- ユーザー要望「題字を参考画像みたいにカラフルに」「キャッチコピーは “遊びながら自然と学んでる” が伝わるものに」「もっと目立たせて、隙間が気になる」対応。`index.html` だけの変更。
- **題字（`#siteTitle`）**: 明朝 → 丸ゴシック極太（`--maru` = Zen Maru Gothic 900・同梱フォント）。1文字ずつ `<span class="title-char">` に割る既存のしくみはそのままで、`TITLE_COLORS`（ピンク→黄→水色→黄緑→むらさき→オレンジ）を順ぐりに当てる。**色を変えたいときはこの配列1行だけ**。濃紺ヘッダーの上で読める明るい色だけを使う・となり同士は必ず別の色、が条件。
- **キャッチコピー**: 「やってみたい」が、きっと見つかる。→ **あそんでるだけ。/ いつのまにか / かしこくなる。** アプリを選ぶ話ではなく「遊んでいるうちに自然と学んでいる」という特徴を先に出す。
  - 見た目は `--pop`（Mochiy Pop One・同梱）、`line-height:1.22`。木目背景に負けないようクリーム色の影を回し、2〜3行目（`<em>`）はオレンジ＋黄色のマーカー線（`linear-gradient` なので行ごとに線が付く）。
  - **字の大きさは「左カラムの幅にちょうど収まる」計算式**: 左カラム = `(min(1440,画面幅) - 96) / 2.5`、1行目「あそんでるだけ。」は8文字ぶんなので `clamp(32px, calc(4.8vw - 4.6px), 64px)`（1440pxで64px・カラム531pxに対し文字514px）。スマホ（760px以下）は1カラムなので `clamp(30px, calc(12vw - 4.4px), 52px)`。「PCでカードとのすき間が気になる」というフィードバックへの対応で、**vwを増やすのではなくこの式で埋めている**。
  - 折り返し位置は `<br>` で固定。**コピーを差し替えるときは1行8文字を基準に、字数が変わったら上の計算式（÷8のところ）も直す**。長い行のままだと変な位置で折り返す。
  - 折り返さないことを 1440 / 1280 / 1100 / 900 / 800 / iPad 1024・768 / スマホ 375・320 で実測確認（1行目の実幅がカラム幅の約9割）。
  - 隙間つぶし: `.hero` の左カラムを `0.9fr → 1fr`、`.hero-kicker` の上下padding を 0、`.hero-eyebrow` の下 14→8px、`.hero-intro` の行間 1.9→1.75。
- Chrome（Browser pane）で 1440×820 / 800×620 / iPhone 375×812 を目視確認。`node release-check.mjs` はエラー0（既知の警告のみ）。

## 検索（Google）に出るようにする — SEOタグ・sitemap・OGP（2026-09-01）

公開URLは **https://kagasen.github.io/kagasen/**（プロジェクトページなので `/kagasen/` が付く）。
「かがせん」「教育アプリ」「知育アプリ」で見つけてもらうために、全ページに検索用タグを入れた。

- **`build-seo.mjs`（新規・ルート）が正**。`apps.js` を読んで、
  ルートと各アプリの `<title>` ＋ 「SEO:auto」ブロック（description / canonical / OGP / Twitterカード）
  ＋ ルートの構造化データ（JSON-LD）＋ `sitemap.xml` をまとめて作る。
  - **アプリを1つ増やしたら `node build-seo.mjs` を走らせるだけ**でタイトルも sitemap もそろう。
  - `<!-- SEO:auto -->`〜`<!-- /SEO:auto -->` と `<!-- LD:auto -->`〜`<!-- /LD:auto -->` の中は手で直さない。
  - 何度走らせても同じ形になる（古いブロック・重複した description / canonical は消してから入れなおす）。
- **りかの4島＋science-island は生成物**なので build-seo.mjs は触らない。
  `science-island/build-islands.mjs` が `../build-seo.mjs` の `seoHead()` を import して同じタグを埋める。
  → 島を直したら `node science-island/build-islands.mjs` のあとに `node build-seo.mjs` の順で走らせる。
- タイトルは全アプリ `{アプリ名}｜かがせんのHAPPYアプリ集` に統一（ブランド名を全ページに置くため）。
- **OGP画像**: `images/ogp.svg` が原本、`node build-ogp.mjs` で `images/ogp.png`（1200×630）に焼く。
  SNSや検索結果のカードはSVGだと出ないところが多いのでPNGにする。@resvg/resvg-js（devDependency）を使う。
- **`robots.txt`** はルートに置いたが、**今の構成では効かない**。robots.txt はドメインの一番上
  （`https://kagasen.github.io/robots.txt`）しか読まれず、そこはこのリポジトリからは触れない。
  sitemap は Search Console から直接出すのが正。独自ドメインに引っこしたらこのファイルが効きだす。
- `release-check.mjs` の項目2を修正: `<link rel="canonical">` は「外部読み込み」ではない
  （検索エンジンへの印であって、ページを開くときに読みに行くものではない）。以前は全アプリが❌になった。
- **未公開アプリの止め方（2026-09-01 追加）**: `apps.js` のそのアプリに `draft: true` を1行足す。
  ポータルのカード（index.html の `visibleApps`）・`sitemap.xml`・JSON-LD から同時に外れる。
  `draft` の行を消して `node build-seo.mjs` を走らせれば公開される。中身が0こになったカテゴリの
  ボタン（🔢算数）は自動で隠れる。
  **2026-09-10: 理科の4島（エネルギー・つぶつぶ・いのち・ちきゅう）の `draft` を外して公開した**
  同じ日に⚡エネルギーの島の中身を**ゲーム1つずつの6アプリ**に分けた（でんき回路／てこのはたらき／
  ふりこゴール／リサイクル工場／ぐるぐる発電／でんじしゃくバトル）。まとめ版の `energy-no-shima/` は
  フォルダだけ生かしてカードは出していない。くわしくは `science-island/HANDOFF.md`
  冒頭の「★ 2026-09-10 ゲーム 1つずつの アプリにも 分けた」節。
  **ただし同日中にユーザーの指示で、理科は「でんき回路」だけ公開に戻した**（ほかの8つは `draft: true`）。
  アプリは37こ・公開は29こ。理科をこれから出すときは、出してよいか必ず確認してから
  `draft` の行を消すこと。
- **走らせ忘れの検出（2026-09-01 追加）**: `release-check.mjs` の項目11が `node build-seo.mjs --check` を呼ぶ。
  アプリを足して build-seo.mjs を忘れると❌で止まるので、検索に出ないまま公開する事故が起きない。
- **Google Search Console 登録ずみ（2026-09-01）**: 所有権の確認ファイルは ルートの
  `google8e4ffe5dee94152e.html`。**消すと確認が外れて Search Console が使えなくなる**ので絶対に消さない。
  sitemap 送信・トップのインデックス登録リクエストまで完了。以後、既存アプリを直すだけなら Search Console 側の操作は不要。
- **残りは人がやる作業**: Google Search Console にサイトを登録 → sitemap を送信 → インデックス登録をリクエスト。
  （登録用の確認ファイル `google*.html` はルートに置けばよい）


## 古いサムネイル3枚を実画面ベースへ刷新（2026-08-24）
- 最近3作（せかいをまわろう／レキシバトル／都道府県の冒険）と同じく、**実際のアプリ画面＋短いキャッチコピー＋大きなアプリ名**の構成へ統一する第1弾。
- 対象: `うごきのきろく` / `自分レベルアップアドベンチャー` / `漢字の冒険`。旧SVGは残してあるが、`apps.js` は新しい1200×780px JPEGを参照する。
  - `images/ugoki-no-kiroku.jpg`（約210KB）: 12種目のカラフルなホーム画面＋青帯。「12しゅもくの『できた！』を きろくしよう」。
  - `images/level-up-adventure.jpg`（約186KB）: 習慣リストと成長キャラ＋紫帯。「まい日の『できた！』で 自分がそだつ！」。
  - `images/kanji-bouken.jpg`（約192KB）: 実際の冒険マップ＋オレンジ帯。「書いて おぼえて 島をぼうけん！」。画面内のKanjiVGクレジットも残している。
- 撮影用の仮プロフィール・習慣データは専用Chromeプロファイルだけに入れ、アプリ本体・ユーザーの保存データ・リポジトリには残していない。
- 今後のサムネイルも **1200×780px / 上部に1行キャッチ / 下部に暗いグラデーション＋白文字のアプリ名 / 実在する画面のみ使用** を基本形にする。

## ホーム上部に「今日のピックアップ」を追加（2026-08-24）
- ユーザー要望「ホーム画面をもっとオシャレに」対応。既存の濃紺ヘッダー＋明るい本文、横スクロール棚、全件グリッドの方針は維持し、最上部に大きな注目枠を追加した。
- `appsData` の日付が最も新しいアプリを自動選択し、タイトル・説明・サムネイル・教科色を表示する。アプリ追加のたびにHTMLを直す必要はない。
- 注目枠のクリックも既存の `goToApp()` を通るため、訪問カウンターの仕様はカードや棚と同じ。
- 検索中またはカテゴリ絞り込み中は注目枠を自動で隠し、一覧に集中できるようにした。
- 安心材料は当初チップで表示したが「ボタンに見える」とのフィードバックを受け、「きょうは どれで あそぶ？」の横に `広告なし・登録なし・iPad対応` を小さな補足テキストとして統合。スマホでは見出しの直下に表示する。
- 背景は最終的に、ユーザー提示のBoard Game Arenaを参考にした明るい白木調へ変更。最初はCSSだけで薄く表現したが「参考画像くらい木目を出したい」との要望を受け、サイト専用に生成した白木テクスチャ `images/light-oak-bg.png`（1254px四方・無圧縮PNG・約1.7MB）へ置き換えた。長いページで `background-size: cover` にすると画像がページ高まで巨大化してぼやけたため、627px四方（Retinaで元画像とほぼ等倍）で繰り返す方式に修正。JPEG圧縮もやめ、木目が細く鮮明に見える画質を優先した。背景固定はスクロール時に手前だけ動いて不快感が出たため使わず、`background-attachment: scroll` でコンテンツと木目が一緒に動く。外部通信なしで表示できる。棚カードは幅244→228px、高さ132→124pxに少し圧縮し、注目枠を足しても一覧まで遠くなりすぎないよう調整した。
- Chromeで 1440×1100 / iPad 768×1024 / スマホ 390×844 を目視確認。`node release-check.mjs` はエラー0（既知のFirebase警告2件のみ）。

## ホーム画面のリニューアル — 濃紺ヘッダー＋明るい本文＋「棚」レイアウト（2026-08-14）
- ユーザー要望「ホームがシンプルでダサい。boardgamearena みたいにカッコよくしたい」対応。**`index.html` だけの書き換え**。`apps.js` / サムネ画像 / Firebase訪問カウンター / お気に入り / 検索・並び替えのロジックは従来どおり。
- 決めたこと（ユーザー合意）: ①カードの**説明文はフル表示のまま**（BGAは説明なしだが情報量を優先）②**棚（横スクロール）＋全件グリッド**の2段構成。
- **配色は「ヘッダーだけ濃紺・本文は明るい」**（＝BGAのゲーム一覧と同じ組み方）。いちど全面ダークで作ったが、ユーザーから「ロゴのところは濃い背景でカラフルに見えてよい／それ以外は明るく、でもBGAのスタイリッシュさは残して」と指示があり現在の形にした。**全面ダークには戻さないこと。**
  - ヘッダー `#0B1020`（虹色ロゴ・検索窓もヘッダー内なのでダーク配色）／本文 `#F5F7FC`＋隅のパステル光＋うすいドット／カードは白＋1pxの線＋やわらかい影。
  - 「ダサい」の原因だった太いオフセット影（`0 6px 0 色`）・点線枠・ふわふわ絵文字はやめて、影と余白で見せる方向に統一。
- 棚は `SHELVES` 配列で定義（**1要素足すだけで棚が増える**）。現在4つ: あたらしい アプリ / おきにいり / よく あそばれてる / せんせい用の どうぐ。
  - 中身が0件の棚は自動で出ない（おきにいり0こ・オフラインで訪問数が取れないときなど）。
  - **棚が出るのは「すべて」かつ検索が空のときだけ**。カテゴリをしぼると一覧だけになる（BGAと同じ）。
  - 棚タイルには NEWバッジを付けない（ほぼ全アプリが該当してうるさいため）。グリッドのカードには従来どおり付く。
- **CSSはこの `<style>` 内で自己完結**させ、Tailwindのクラスは新規に使っていない（`tailwind.css` は静的purgeビルドなので、書き足したクラスは効かない＝脱CDNのときの事故と同じ轍）。`tailwind.css`/`fonts.css`/`vendor/lucide.min.js` の読み込みは温存。
- 教科カラーは `CARD_COLORS` に **`main`（明るい・枠/下線）・`deep`（濃い・白文字が読める）・`tint`（うすい・タグの地）** の3色セット。カードの枠・タイトル下線・「あそぶ」ボタン（main→deepのグラデ＋白文字）・教科タグ（tint地にdeep文字）が全部これで連動する。**白背景なので `deep` を使わないと文字が読めない**（黄色系がとくに危ない）。
- 訪問カウンターの読み込み完了後は **`renderShelves()` と `updateCountDisplay()` だけ**呼ぶ（`renderApps()` を呼ぶとカードの登場アニメがやり直しになる）。
- タップターゲット: カテゴリチップ48px・あそぶボタン48px・★ボタン48px。棚の左右矢印は `pointer:fine` かつ幅1024px以上のときだけ表示（iPadはスワイプで送る）。
- 検証: Chrome で 1400幅 / iPad(768) / スマホ(375) を目視、カテゴリしぼりこみ・検索0件・お気に入りON/OFF・名前順/新着順・棚の増減・ボタンとタグの実際の色をDOMで確認。コンソールエラー0、`node release-check.mjs` はエラー0（Firebaseの既知警告2件のみ）。

## 全アプリに「アプリ集へもどる」リンク（2026-08-14）
- ユーザー要望「ホーム画面にアプリ集へのリンクを。他のアプリにもなければ作って」対応。**28アプリすべてにポータル(`../index.html`)へのリンクが入った状態**にした。
- もともと有ったのは5つ（rekishi-battle / typing / vision-training / shinmatorikusu / marumarusaitekikai）。のこり23に追加。
- **ugoki-no-kiroku** はホーム下部に アプリのデザインに合わせたカード（`.portal-card`）を追加。
- **他22アプリ** は共通スニペット（`id="kagasen-home"` の `<a>`＋`<style>`）を `</body>` 直前に貼る方式。アプリ側CSSの影響を受けないよう id で完結・`z-index:2147483000`・タップ高さ44px・`@media print` で非表示・セーフエリア対応。
- 下に自前のバーがある3アプリだけ位置を調整（level-up-adventure=下ナビの上 / taiikusakusennbo-do=チームボタンの上 / taiiku-league=左上）。**22アプリぜんぶスクショで重なりを目視確認ずみ**。
- リンク先は `../index.html`（`../` だと file:// でフォルダ一覧になるため）。各アプリのSWスコープ外なのでキャッシュにも干渉しない。
- 変更した24アプリの `sw.js` の CACHE をすべて繰り上げ（release-check エラー0）。

## 古いiPad対応 — ES2020構文の一掃（2026-07-10）
- **「iPadでいくつかのアプリが開けない」報告の原因**: `?.`（オプショナルチェーン）と `??`（Null合体）は ES2020構文で、**iPadOS 13.3以前のSafariはパースできない**。構文エラーは1箇所でもスクリプト全体を殺すので、該当アプリは真っ白／無反応＝「開けない」になっていた。
- 直した箇所:
  - `ugoki-no-kiroku/index.html` … `??` 2箇所（esc関数・グラフのpoints）
  - `level-up-adventure/index.html` … `?.` 3箇所（プロフィール名の表示。**テンプレートリテラルの `${…}` 内**だったのが盲点）
  - `shiritori/index.html` … JSX内の `?.` 4箇所（**ブラウザ内Babelは JSXだけ変換して `?.` は素通し**なので、text/babel の中も古い書き方が必要）
  - 各アプリの `backup-kit.js`（9本）… `??` 1箇所 → 全部 v3 に繰り上げ
  - `sikou-tool-app` … esbuild のビルドに `--target=es2017` を追加して再ビルド（root の build:typing は元から es2017 だった）
  - `sakkanojikan` … Play CDN のJS版ランタイム `tailwind.min.js`（ES2020を含む）を静的 `tailwind.css` へ移行
  - 脱CDNの残骸 `tailwind.config = {…}` ブロック（ポータル・vision-training×4・level-up-adventure）を削除（`Can't find variable: tailwind` エラーの発生源。静的CSS化後は無意味）
- **再発防止**: `release-check.mjs` に項目10（ES2020+構文の検出）を追加。今後 `?.`/`??` 等を書くと❌になる。**このアプリ集の自前JSは「ES2017＋α（ES2018のスプレッド・ES2019のcatch省略まで）」縛り**。
- 検証: Playwright WebKit で全26アプリを起動しコンソール/ページエラーゼロ・スクリーンショット目視、書き換え箇所は実操作（レベルアップの名前表示・しりとりの対戦開始・うごきのきろくの初回スタート）で通過を確認。

## 脱CDN（2026-07-06）
- `tailwind.css` … 静的ビルド（content=index.html,apps.js・font-pop/maru と natural-brown のconfig付き）。
- `fonts.css` + `fonts/` … Mochiy Pop One / Zen Maru Gothic の**同梱サブセット**（リポジトリ全体の使用文字3232字・SIL OFL）。全アプリ共通のセットなので、作り直すときは他のアプリと一緒に（kanji-bouken/build-fonts.mjs の流儀）。
- `vendor/lucide.min.js` … unpkg の置き換え。
- **Firebase（訪問カウンター）は温存**。オフライン時は fbOk ガードでカウントだけ止まり、ポータル自体は動く。

## サイエンスアイランド「リサイクル工場」学習・演出強化（2026-08-29 / cache v20）
- `science-island/index.html` の60秒パズルを拡張。10個以上の連鎖で出る必殺技は、プラ4種／粗大4種／燃える4種／金属5種を順番に見せ、同じごみでも複数の行き先があることを遊びながら学べる。
  - プラ: ボトルtoボトル、卵パック、ユニフォーム、ペン・回収ボックス。
  - 粗大: 中古販売、修理して再使用、家具へのリメイク、再生ボード。
  - 燃える: 発電、温水・暖房、焼却灰からエコセメント、資源化できない灰の管理型最終処分。
  - 金属: 新しい缶、自動車鋼板、レール、橋・建物、家電の鉄部品。
- 盤面に低確率で「すてちゃダメ」カードを出す。金のアクセサリー、まだ着られる服、使わないスマホをタップすると連鎖とは別の救出演出になり、修理・リユース・適正回収先と短い理由を表示して+150pt。通常のごみ連鎖には混ざらず、落下・補充・手詰まり判定も対応済み。
- 終了画面にエコポイント、葉1〜5枚のエコランク、再生品数、救出数、必殺回数、最大連鎖、種類別処理数、ランダム豆知識を追加。得点が何を表すかを結果として読める形にした。
- 学習文言は公的・業界団体の一次情報を根拠に調整。焼却灰を一律に「埋立地へ利用」とせず、資源化できる灰はエコセメント、できない灰は管理された最終処分場へ、と区別している。燃えるごみもマテリアルリサイクルではなく熱回収として表示。
- 必殺演出用にImageGen（built-in）で、発光するガラス製リサイクル装置・歯車・コンベヤーを描いた文字なしのゲーム素材を生成し、`science-island/images/recycle-transform-machine-v1.png` に配置。プロンプト方針は「上質な科学教育ゲーム、3Dトイ調、中央の変換装置、シアン／エメラルド／金、人物・ロゴ・文字なし」。
- 実ブラウザ確認: 10連鎖で495pt・盤面56個へ正常補充、衣服救出で+150pt、強制終了結果で3560pt／循環マスター／再生品18／救出3／必殺4／最大14連鎖を表示。NaNや盤面欠落なし。
- 確認画像: `science-island/recycle-factory-long-chain-stage3-10chain-preview.png`、`science-island/recycle-factory-rescue-preview.png`、`science-island/recycle-factory-eco-result-preview.png`。
- `node science-island/verify.mjs` は5854件すべてOK。`node release-check.mjs` はエラー0、既知のFirebase外部読み込み警告2件のみ。`science-island/sw.js` は `science-island-cache-v20`、生成画像もプリキャッシュ対象。

## リサイクル工場「7分別＋危険ごみ」とSafariタップ修正（2026-08-30 / cache v22）
- ステージを3→5→7分類へ段階的に難しくした。追加は `can`（空きかん）、`bottle`（ガラスびん）、`glass`（ガラス・陶器）。最終面は、もえる／プラ／かん／びん／ガラス陶器／そだい／その他金属の7分類。似ている PET・びん、かん・金属、びん・割れガラスを見分ける設計。
- 電池・スプレー缶・ライター・割れガラスは赤い「危険！タップ」カード。タップ後に3択の出し方を答え、正解 +200pt、誤答 -80pt・-3秒。名称や出し方の地域差を必ず注記。従来の金・衣服・スマホ救出も残している。
- `FC_SPECIALS` に、かん4種・びん4種・ガラス陶器3種を追加。びん→色別カレット／新しいびん、缶→鋼板・アルミ製品、陶器・割れガラスはびん回収へ混ぜず安全に分別、を表示する。
- 終了時の汎用「パーフェクト／クリア」モーダルはリサイクル工場だけ出さない。エコ結果画面の中に豆知識と「ステージへ」「もういちど」を置き、記録保存は `fcStoreResult()` で静かに行う。
- SafariでSVG内の透明タップ領域が不安定だったため、クイズ3択と結果2ボタンを `#fc-controls` 内の通常のHTML `<button>` に移行。`cur.done` で盤面クリックが止まっても結果ボタンは動く。
- 回答後の解説カードがDOMに残ったままタイマーだけ再開する不具合を修正。`cur.sortQuiz` が存在する間は回答前・回答後とも時間停止し、解説終了時に `cur.fcNeedsRender` で必ず再描画してから再開する。
- Chrome実操作: 電池クイズの3ボタン表示、正解後900msでも残り59.816秒のまま、誤答時のみ -80pt/-3秒、終了後900msでも汎用モーダルなし。「もういちど」で `playing=true / left≈59.83 / done=false`、「ステージへ」で `screen-island` へ遷移。盤面56個維持、SVGにNaN/undefinedなし。
- 確認画像: `science-island/recycle-factory-danger-sort-quiz-preview.png`、`science-island/recycle-factory-danger-sort-correct-preview.png`、`science-island/recycle-factory-sorting-result-v21-preview.png`。リサイクル工場の専用検証はエラーなし、`release-check.mjs` はエラー0（既知のFirebase警告2件）。

## リサイクル工場 Safari長押しタップの根本修正（2026-08-30 / cache v23）
- クイズ中の `fcRender` が `cur.sortQuiz` を条件に毎フレーム `renderFactory()` を呼び、選択肢のHTMLボタンを約16msごとに交換していた。人の `touchstart`〜`touchend` の間に要素が消えるためSafariでclickが成立しなかった。`cur.sortQuiz` を毎フレーム再描画条件から外し、開始・回答・解説終了時だけ更新するよう修正。
- `#fc-controls` はクイズ／結果中だけ `.on` で `pointer-events:auto`。さらに `touchend` を直接処理し、後から合成されるclickは700ms抑止して二重回答を防ぐ。
- Chrome DevToolsの `Input.dispatchTouchEvent` で140msの指タップを再現。クイズ正解／誤答、「もういちど」、「ステージへ」の4操作がすべて成功。回答後の解説中も59.816秒のまま停止。「もういちど」は `playing=true / done=false / left≈59.83`、戻るは `screen-island`。
- リサイクル工場専用検証エラーなし、公開前チェックはエラー0。`science-island/sw.js` は `science-island-cache-v23`。

## ポータルのサムネイル見直し作業（2026-08-30・作業中）
- いちど全33アプリへHTML重ね表示の小さなキャッチコピーを追加したが、意図は「歴史バトルのように、コピーも画像そのもののデザインとして大きく読みやすく入れる」ことだった。左上の小さな帯は不要とのフィードバックを受け、`catchCopy`、`.shelf-catch`、`.thumbnail-catch`、追加グラデーションをすべて撤回した。
- `images/thumbnails-v2/` の候補画像25枚は、今後1アプリずつ比較・修正するため残している。ポータルでは現時点でもこの候補画像を参照しているが、キャッチコピーは一切重ねていない。
- 今後は上記「古いサムネイル3枚を実画面ベースへ刷新」にある **1200×780px / 上部に大きく読みやすい1行キャッチ / 下部に暗いグラデーション＋白文字のアプリ名 / 実在する画面を優先** を基準に、1件ずつユーザー確認しながら仕上げる。
- 都道府県の冒険は400×260の元SVGを横長カードへ `cover` 表示していたため上下が切れていた。`apps.js` にこのアプリだけ `imageFit: "contain"` を設定し、棚・一覧の両方で `fit-contain` クラスを付けるよう修正。左右余白は空色〜白〜薄緑の背景で補い、元画像全体を常に表示する。984px幅の2列表示で、進捗・問題・地図・青いコピー帯・「ちずをタップ！」がすべて枠内に収まることを確認。
- 漢字の冒険は、実際の「個」の練習画面を参照画像としてbuilt-in ImageGenで、子どもがタブレットへ「個」を書いている場面を生成。生成段階では文字を入れず、1200×443へ整えた後に「書いておぼえて、漢字マスターへ！」をヒラギノ角ゴW8で赤い帯へ正確に焼き込んだ。完成画像は `images/kanji-bouken-thumbnail-v2.jpg`。ポータルもこの画像を参照する。

## 画像を WebP にして 容量を減らした（2026-09-10）

きっかけは「アプリを増やしていくと 容量がいっぱいで動かなくなるのが困る」という相談。
調べたら、危ないのは localStorage（各アプリ数KB〜数百KB・Safariの上限5MB）ではなく
**PWAのキャッシュ（Cache Storage）** のほうだった。

測ったときの数字:
- sw.js が precache するアセットの合計が **全アプリで390MB**（ugoki-no-kiroku 単独で291MB）
- ugoki-no-kiroku/images は **366枚・427MB**、1枚平均1.2MBの1774×887 PNG
- GitHub Pages に公開される合計は **600MB**（サイト容量の上限は1GB）

なぜ困るか:
1. iPad Safari は容量を超えると **オリジンごと** データを捨てる。
   `kagasen.github.io` は全アプリで1つのオリジンなので、
   localStorage に入っている子どもの記録まで道づれになる。
2. `sw.js` の install は `addAll` なので、1ファイルでも落ちると **install 全体が失敗**＝オフラインが効かない。
3. 初回起動で390MB落とすのは、教室のWi-Fiでは現実的でない。

### やったこと
`build-images.mjs`（新規・ルート）を追加し、全アプリの png/jpg を **WebP 品質90・長辺1600px** に変換した。
絵は変えていない。見た目は同じで、ファイルだけ軽くなる。

```
node build-images.mjs <アプリのフォルダ名> [--apply]
node build-images.mjs --all [--apply]      # 前提: python3 と Pillow
```

- `--apply` なしだと変換だけ（下見）。`--apply` で html/js/json の `〜.png` を `〜.webp` に書きかえる。sw.js の ASSETS もこれで直る。
- `${...}.png` のように名前を組み立てている参照は機械で直せないので、実行後に一覧で出す（**手で直す**）。
- さわらないもの: `ogp` / `favicon` / `apple-touch-icon` / `icon` で始まるファイル名（SNS・ホーム画面用は png/jpg のままでないといけない）。
  ★フォルダ名で判定すると `home-icons/` のようなふつうの絵まで巻きこむ。**ファイル名だけで判定**している。
- git が無視するファイル・git がまだ知らないファイルは変換しない（作業用の素材フォルダやスクリーンショットを巻きこまないため）。
- WebP にすると逆に大きくなる画像は変換せず、元のまま使う（hantai-no-kotoba の JPG 119枚はこれ）。

### 結果
- 変換した画像: **514MB → 41MB（7.9%）**
- GitHub Pages に公開される合計: **600MB → 129MB**
- 元のPNG 454枚（510MB）はリポジトリから外した。**git の履歴には残っている**ので `git show` でいつでも取り出せるし、
  念のため Mac の `~/kagasen_projects/画像の元データ/` にも同じ構成でコピーしてある（バイト単位で一致を確認ずみ）。

### これから画像を足すときの手順
1. 元画像（png）を置いて `git add` する
2. `node build-images.mjs <アプリ名> --apply`
3. 名前を組み立てて参照している箇所があれば手で `.webp` に直す
4. `sw.js` の CACHE を繰り上げる（§4）。science-island 系は `build-islands.mjs` が中身のハッシュから自動生成するので手で直さない
5. 元のpngは `~/kagasen_projects/画像の元データ/` へ移す

### まだ残っている宿題（今回はやっていない）
- **precache に画像を全部載せている**。`sw.js` の fetch は「取ってきたものをキャッシュに入れる」作りなので、
  ASSETS から画像を外せば「起動に必要なものだけ先に確保・画像は使ったときに貯まる」に変えられる。
- **release-check.mjs に容量の見張りがない**。「precache合計が◯MBを超えたら警告」「1ファイルが◯KBを超えたら警告」を足すと、
  アプリが増えても自動で気づける。
- ポータルの `images/`（サムネイル26MB）は build-ogp.mjs / apps.js と絡むため今回は対象外。
