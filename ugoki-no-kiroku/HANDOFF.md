# うごきのきろく — 引き継ぎメモ

小学生向け運動チャレンジ＆成長ログアプリ。完全オフライン（localStorage）・PWA。

## 構成
- `index.html` … 本体（CSS/JS込みの単一ファイル、ビルド不要）
- `backup-kit.js` … きろくの「バックアップ＆ひきつぎ」共通部品（3アプリ同梱コピー方式。下記参照）
- `manifest.json` / `sw.js` / `icon.svg` … PWA。**アセット更新時は sw.js の `CACHE` バージョンを繰り上げる**（v1→v2…）
- ポータル登録は ルートの `apps.js`、サムネイルは `images/ugoki-no-kiroku.svg`

## 大前提（設計合意）
- サーバー・API・CDNなし。フォントもシステムフォント（オフライン初回起動でも崩れない）
- データは localStorage キー `ugokiNoKiroku_v1` に単一JSONで保存
- 表記はひらがな中心、タップターゲット48px以上

## 12種目と記録方式
| 種目 | 方式 |
|---|---|
| なわとび | 技19種×連続回数（自己ベスト管理）＋30秒はやまわしチャレンジ（まえ/うしろ、折れ線グラフ） |
| てつぼう | 検定20技チェック → 級判定（2技=1級きざみ、20技=特級） |
| マットうんどう | 10技チェック（難易度★・コツ付き） |
| とびばこ | 6技 × 何段とべたか（2〜8段、最高段を保持） |
| すいえい | 検定22段階（20級→2段）チェック＋泳法別距離ログ（累計バー）。20級「かおに水」〜13級あたりは小1向けのやさしい水慣れ課題。旧12段階(id: s10〜sS2)のデータは load() 内で新id(sw〜)へ自動移行 |
| マラソン | 距離・タイム入力、累計km、目標バー（10km→42.195km→100km）、グラフ |
| サッカー/バスケ/バレー/野球/ドッジ/リレー | `BALL_SPORTS` 定義の汎用チャレンジカード（**各15個**・2026-08-14に7→15へ増量）。type:'count'=回数・距離・秒の自己ベスト（`low:true` はタイム系で小さいほど良い）、type:'check'=できたチェック。ページは `viewBallSport()` が共通描画 |

## 級・検定データの出典
栃木県教育委員会「みんなが使えるチャレンジカード集」（小学生のための体力つくりの手引き, 平成21年）を参考。
- 鉄棒検定カード（20技・得点→級）
- なわとびチャレンジカード（技ごとの回数→級。二重とび100回=特級、50回=準特級）
- 水泳がんばりカード（10級→2段）
- 30秒はやまわしの目安（70回→二重とび可、90回→連続二重、100回→三重とび）
- なわとび級表は原本の表が読み取りづらい箇所があり一部補間している

## ゲーミフィケーション
- XP: 記録+10 / 技初達成+30 / 自己ベスト+20 / バッジ+50 / 7日連続+100
- レベル: 必要XP = 100 + (レベル-1)×25 の累積
- キャラ進化: たまご(Lv1)→ひよこ(Lv3)→とり(Lv6)→つばさ(Lv11)→でんせつのとり(Lv21)。SVGは `charSvg()` 内
- バッジ143種（`BADGES` 配列、`cat` でカテゴリ分け、`test(S)` 関数で判定。きろく10/れんぞく5/レベル5/なわとび24/てつぼう10/マット10/とびばこ8/すいえい14/マラソン8/ボール6種目×6/ステップ5/スペシャル8）。獲得演出はモーダル＋自前コンフェッティ（`confettiBurst()`）。旧31種のidはすべて新100種に残してあるので獲得済みバッジは消えない
- 連続記録: 「今日の日付での記録」のみ streak にカウント。7の倍数日で+100XP
- **取り消し時はXPも巻き戻す**: チェック外し・水泳検定取り消し・マラソン記録削除は、対応する records の1件を `removeRecordXp()` で消して totalXp から差し引く（レベルは totalXp 由来なので自動で戻る）。ever_◯◯ からも外すので、付け直せば初達成+30も再付与される＝トグルの往復でXPは増減ゼロ。獲得済みバッジは取り消さない方針（バッジXPは初回のみなので farming 不可）
- 全技にSVGイラスト付き（`TETSU_ART`/`MAT_ART`/`TOBI_ART` は共通パーツ製の人＋オレンジの動き矢印、なわとびは `nawaArt()` がパラメータ{back,cross,multi,feet,label}から生成）

## バックアップ＆ひきつぎ（backup-kit.js・2026-07-04）
- せってい画面「💾 きろくを まもる」→ `BackupKit.open()`。書き出し＝封筒JSON `{kagasenBackup:1, app, appName, exportedAt, data}` のファイルDL＋テキストコピーの2方式。読み込み＝ファイル or 貼り付け → 検証 → プレビュー（いまの きろく vs ファイルの きろく：なまえ/レベル/XP/きろく数/バッジ数/最終記録日）→ 確認して全置換。
- **安全設計**: 壊れたJSON・別アプリの封筒（app不一致）は既存データに触れず拒否。置換直前に現データを `ugokiNoKiroku_v1_mae` へ1世代退避（誤操作時は手で戻せる）。旧「データをほぞん」の封筒なしJSONも `looksLike`（profile＋records配列）判定で後方互換読み込み可。外部送信なし。
- **共通部品の流儀**: `backup-kit.js` は3アプリ（ugoki / level-up-adventure / kanji-bouken）に同一ファイルを同梱コピー（three.min.js と同じ方式）。アプリ固有部分（appId・storageKey・looksLike・summarize）は各 index.html の `BackupKit.init({...})` に置く。**部品を直したら3フォルダ全部に配り直し、各 index.html の `?v=` と各 sw.js の CACHE を繰り上げる**。
- 旧 `exportData()`（素のJSONダウンロード）はこの部品に置き換えて撤去済み。

## データ構造（localStorage）
```
{ profile:{name,grade,charName,totalXp,createdAt},
  records:[{id,sport,date,label,xp}],
  nawatobi:{best:{技id:回数}, speed:[{date,mae,ushiro}]},
  tetsubo:{done:[]}, mat:{done:[]}, tobibako:{dan:{技id:段}},
  suiei:{kyu:[], log:[{date,stroke,m}]}, marathon:{log:[{id,date,m,sec}]},
  badges:[], streak:{last,count,best}, ever_◯◯:[] }
```

## sw.js の activate 修正（2026-07-07）
- **バグ修正**: activate の古キャッシュ掃除が `k !== CACHE`（自分以外全部削除）だったため、同一オリジン（GitHub Pages）で他アプリのオフラインキャッシュを消していた。自アプリのプレフィックスだけ削除する条件（`k.startsWith('(自分のキャッシュ名)-') && k !== CACHE`）に修正。CACHE名・ASSETSは不変（キャッシュ繰り上げ不要、sw.js自体の更新はバイト差分で自動配布される）。

## 初回セットアップのボタンが押せない不具合（2026-07-08）
- **症状**: iPadで初回モーダルに なまえ／あいぼうのなまえ を入力後、「ぼうけんスタート!」が押せず固まる。
- **原因**: モーダルが画面中央固定（`.modal-overlay` は `align-items:center`）。iOSのソフトウェアキーボードが出ても `position:fixed` は縮まないため、下寄りのボタンがキーボードの裏に隠れてタップできなかった（PC/実機キーボードでは再現しない）。
- **修正**: 初回モーダルの overlay に `form-modal` クラスを付け、`align-items:flex-start`＋`overflow-y:auto` で上寄せ＋スクロール可にしボタンをキーボードの上に出す。あわせて なまえ／あいぼう 入力欄で Enter(かんりょう)キーでもスタートできるよう `start()` を共通化してバインド。celebration系モーダル（入力なし）は従来どおり中央のまま。
- キャッシュ: index.html 変更のため `sw.js` の CACHE を v8→v9 に繰り上げ。

## backup-kit v2（2026-07-07）— このHANDOFFが部品の source of truth
- **v2の変更**: localStorage キーが複数あるアプリ用に `collect()`（現状を1オブジェクトに集約）/ `restore(data)`（書き戻し。`_mae` 退避も restore 側の責任）フックを追加。両方セットで指定すると storageKey は使われない。従来の単一 storageKey 方式は挙動不変（後方互換・このアプリはこちら）。
- **同梱アプリは9個に拡大**: ugoki-no-kiroku / level-up-adventure / kanji-bouken（従来3）＋ typing（3キー・collect/restore）/ shukudai / shinmatorikusu / classroom-board（2キー・collect/restore）/ taiiku-relay / sikou-tool-app。sakkanojikan は自前のファイル保存があるため対象外（同HANDOFF参照）。
- 部品を直したら**9フォルダ全部に配り直し**、各エントリHTMLの `?v=` と各 sw.js の CACHE を繰り上げること（今回 v1→v2、ugoki-cache v7→v8）。
- 検証済み（2026-07-07）: typing 3キーの書き出し/読み込み/退避、classroom-board 2キー（キー削除含む）、単一キー4アプリの一周、v2でも既存 ugoki の書き出しが従来どおり動くこと。

## しゅもくアイコンとカードのせつめい文（2026-08-14）
- ホームのカードが「チャレンジ7こ」のままだった件 → **`sportSub(id)` がデータから作る**ように変更（ボール系は `BALL_SPORTS[id].skills.length`、なわとび/てつぼう/マット/すいえいも配列の長さから）。**わざを増やしても数がズレない**。SPORTS の `sub` は とびばこ・マラソンのフォールバックとしてのみ使う。
- **てつぼうのアイコンを自前SVGに**（絵文字に鉄棒がなく、🤾だと ドッジボールと同じ絵になってしまうため）。`SPORTS.tetsubo.icon` にSVG文字列を持たせ、`sportIcon(sp)` が icon優先→なければ絵文字 を出す。使用箇所: ホームのカード／あしあとの色カード／きろくのタイムライン／「これからチャレンジ」／てつぼうページの見出し。
- 他のしゅもくも同じやり方でSVG化できる（`icon` を足すだけ）。CSSは `.em.ic svg{width:1em;height:1em}` で絵文字と同じ大きさにそろえてある。
- キャッシュ: `sw.js` の CACHE を v13→v14 に繰り上げ。

## ボール6しゅもくを7→15チャレンジに増量（2026-08-14）
- ユーザー要望「7個は少ない、やり込み要素として15個くらい」対応。**6しゅもく×8こ＝48チャレンジ追加**（ボール系ごうけい 42→90）。既存の7こは1つも消していない（id・保存データそのまま）。
- 追加例: サッカー=スローイン/かべパス30びょう/コーン5ほんジグザグ(タイム)/ロングキック(m)/あたまリフティング、バスケ=バウンドパス/ピボット/フリースロー10ぽん中/足のあいだ8の字、バレー=レシーブ/3人でパス/ネットごしラリー/スパイク/ブロック、やきゅう=グラブでとる/ベースひとまわり(タイム)/まとあてスロー/ふたりでアウト、ドッジ=そとやへパス/フェイント/ジャンプキャッチ、リレー=30m・100mそう/もも上げ/コーナー/はしりはばとび/20m5おうふく。
- **ならびは `ballSorted()` が ★(st) の昇順にならべる**ので、あとから足したチャレンジも「やさしい順」の正しい位置に入る（配列の末尾に足すだけでOK。**新チャレンジは st を必ず付ける**）。
- 絵はすべて既存の共通パーツ（aGround/aBall/aArr/aMini/aRunFig 等）の組み合わせ。**新しい描画関数は増やしていない**ので、パーツを直せば全部に効く流儀は保たれている。
- 新チャレンジ48こ ぜんぶに スモールステップを1こずつ用意（ステップ総数 259→307）。
- バッジ: しゅもくごとに「◯◯はんぶん!（チャレンジ8こ）」を追加＝6こ増（137→**143**）。「はかせ（ぜんぶ）」は15こ必要になり、やり込みがいが出た。判定用に `ballDone(s,sport)`（できた数）を追加。
- 検証済み（2026-08-14）: 17ルートの描画エラー0／各しゅもく15こ・ID重複なし／新48枚の絵を一覧で確認／ステップID重複なし・全チャレンジにステップあり。
- キャッシュ: `sw.js` の CACHE を v12→v13 に繰り上げ。

## デザイン刷新・SVG品質アップ（2026-08-14）
- ユーザー指摘「シンプルすぎてワクワクしない／SVGのクオリティが低い」対応。**絵の作りかたは変えず、共通パーツの質を上げて全体を底上げ**する方針。
- **キャラクター(`charSvg`)を5だんかい全部かきなおし**: radial/linearGradient・ハイライト・ほお・白目＋黒目＋2つのハイライト・くちばしの2色・つばさの重ね・きらめき(`sparkle`)。共通の小関数(`eye/blush/beak/sparkle/gloss`)にまとめてある。**グラデーションidは `_uid` で毎回ユニーク化**（同じ画面に2つ出ても混ざらない）。
  - サイズ指定なしで呼ぶと**インラインstyleを付けない**＝CSSで大きさを制御できる（ホームのレスポンシブ用）。モーダルは `charSvg(lv,110)` のように指定するとその大きさで固定。
- **ホーム画面を「そらとおかのステージ」に**: `.hero` に 太陽・雲2つ(ゆっくり流れる)・丘 を追加（`.sun/.cloud/.hill`、`.hero>*{z-index:2}` で本文は前面）。XPバーはつやと流れるしま模様、Lv.チップは立体、ページ背景にうっすら水玉。
- **しゅもくカードを立体ボタン化**＋**しんちょくバー**: `box-shadow` で下側にふちを作り、押すと沈む。`sportCardsHtml()` が `dekitaOf(id)` を見て、**1つでもできていれば「せつめい文」→「バー＋できた数」に変わる**（マラソンは「ぜんぶで◯km」チップ）。使うほどカードが育って見える。
- **わざイラストの共通パーツを強化**（`aHead/aTorso/aLimb/aArr/aTip/aBar/aMat/aGround/aBox`）。手足は「こい輪郭＋はだ色の中身」の2本がさね、顔（目・口・ほお）を追加、矢印にも輪郭、用具に影とハイライト。**個々のわざの座標は1つも変えていない**ので、100いじょうの絵がまとめて良くなる。ここを直せば全部に効く＝今後もこの流儀で。
- スマホ幅(≤400px)用の `@media` を追加（キャラ104px・XP行が折り返さない）。**Chrome headless は最小幅500pxまでしか再現できない**ので、375px幅の確認はブラウザのモバイル表示で行うこと。
- 検証済み（2026-08-14）: ホーム/わざ一覧/モーダルの表示、375px・500px幅で横はみ出しなし、キャラ5だんかい、わざイラスト24種の一覧チェック、構文OK・ES2020構文なし。

## スモールステップ（2026-08-14）
- ユーザー要望「いきなり前転ではなく ゆりかご から。水泳も 顔つけ から。もっと細分化して」対応。**全12しゅもく・120わざに 計259のスモールステップ**を追加。
- **データは `STEPS` 1か所に集約**（`STEPS[わざのid] = [{id,name,kotsu}]`）。わざidは全しゅもくでユニークなのでフラットな1オブジェクト。**ステップを増やすときはここに1行足すだけ**でUI・XP・カウント・あしあとに反映される。マラソンだけ わざがないので疑似id `mara` を使う。
- **見せ方**: わざカードの中の折りたたみ（`stepsHtml(sport, skillId)`）。ふだんは閉じていて、つまずいた子だけ開く＝既存のわざ一覧の見た目を壊さない。開閉状態は `openSteps` にセッション中だけ保持（保存しない）。
- **保存**: `S.steps=[達成したステップid]`（旧データは `load()` の既定値補完で自動的に `[]` が入る＝移行不要）。取り消しは付与と対称（`removeRecordXp` ＋ `ever_steps` から除去）で、トグル往復のXPは増減ゼロ。**検証済み**。
- **XP**: ステップ1つ +25XP（きろく10＋ステップボーナス `XP.step`=15）。本わざの初達成(+40)より小さくしてある。
- **級への反映（ユーザーが明示的に選択した仕様）**: 「ステップぜんぶクリアしたわざ＝**0.5わざ**」として できたわざ数に加算する。
  - てつぼう … `tetsuPoints()` に入り**実際に級が上がる**（本わざ2こ＝1級／ステップ完了4わざ＝1級）。**特級だけは本わざ20こ必須**（出典カードの意味を守るため）。
  - マット・とびばこ・ボール6種目 … ヘッダーの「＋ステップだけ Nわざ」表示とゲージに反映。
  - **なわとび・すいえいは級を動かさない**。級の定義が「回数」「検定そのもの」で、出典（栃木県チャレンジカード）の表を書きかえることになるため。かわりに ステップ数の表示と あしあとの「できたことリスト」に出す。この判断はユーザーに説明済み。
- **あしあと連携**: できたことリストに「(わざ名) ステップ ぜんぶ!」と「🪜 スモールステップ Nこクリア」をおまけ項目(`ex:1`)として追加。**わざがまだ0でもカードが出る**ので、苦手な子ほど「できたこと」が見える。
- バッジ5個追加（`cat:'ステップ'`、1/10/30/60こ＋あるわざのステップ全クリア）。**BADGES は 132→137**（PVの字幕「132しゅるい」は古くなった）。
- 命名の注意: 既存の回数ステッパー `stepCount(id,d)` と名前が衝突するので、ステップ側は **`stepDoneCount(id)`** にしてある（同名関数を作ると＋/−ボタンが壊れる）。
- 検証済み（2026-08-14）: 17ルート全ての描画エラーなし／級の反映（ステップ4わざ分で10→9きゅう、本わざ達成後に二重カウントされない）／XP往復ゼロ／旧データ（stepsキーなし）の読み込み／スマホ幅375pxで横はみ出しなし・タップターゲット48px以上／ES2020構文なし／release-check エラー0。
- キャッシュ: `sw.js` の CACHE を v11→v12 に繰り上げ。

## あしあとの「できたことリスト」（2026-08-13）
- ユーザー要望「いまできたことが一覧でわかるように・種目ごとに色分け」対応。**あしあと**ページを2タブ構成にした（`.section-tabs` を再利用、`progressTab` はセッション内のみ保持・既定は できたこと）。
  - **🌟 できたこと**（新規）… しゅもくごとの色カード。ヘッダー（色バー）をおすと そのしゅもくのページへ。
  - **📅 さいきんのきろく**… 従来のタイムライン（14日ぶん）。中身は変更なし。
- 色分けは `SPORTS` に足した **`col`（こい色＝バー・文字）/`bg`（うすい色＝チップ背景）**。`cls` のグラデーションとそろえてある。**しゅもくを増やすときは col/bg も必ず足す**（無いとチップが透明になる）。
- 「いまできること」の収集は **`dekitaOf(sportId)` 1か所**に集約。返り値 `{items:[{nm,val,ex}], total, sub}`。`ex:1` は おまけ（30びょうはやまわし・すいえいの累計きょり・マラソンのまとめ）で **「X/Y」のかずには数えない**。描画は `dekitaHtml()` が共通で回すので、しゅもく追加は dekitaOf に1ブロック足すだけ。
- 表示内容: なわとび=わざごとのベスト回数＋到達級／てつぼう・マット=できたわざ／とびばこ=わざごとの最高段（2だん以上）／すいえい=ごうかくした級＋泳法別るいけい／マラソン=るいけい・かいすう・いちばんながい日／ボール6種目=できたチェックと自己ベスト。
- 未着手のしゅもくは下の「🌱 これからチャレンジ」にグレーのボタンでならぶ（次の目標が見える）。
- 検証済み（2026-08-13）: ダミーデータで12種目の表示・タブ切替・ヘッダーからの遷移・きろく0件の空状態・スマホ幅375pxで横はみ出しなし・ES2020構文なし・release-check エラー0。
- キャッシュ: index.html 変更のため `sw.js` の CACHE を v10→v11 に繰り上げ。

■ サムネイル刷新（2026-07-12）: images/ugoki-no-kiroku.svg をアプリ画面の再現（実画面スクショ風のSVG）に差し替え。ユーザー要望「サムネがダサい」対応。旧版は git 履歴にあり。
